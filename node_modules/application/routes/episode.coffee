Episode = require 'application/models/episode'

module.exports = (req, res) ->

  await Episode.findByShowIdAndEpisodeNumber(req.params.showId, req.params.episodeNumber, defer(episode))

  # Return data
  res.send(episode)
